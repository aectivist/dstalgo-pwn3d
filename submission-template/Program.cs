// Placeholder only -- used to warm the NuGet/build cache when the Docker
// image is built (see Dockerfile). judge-host overwrites this file with
// the actual student submission for every real request; this file itself
// is never shipped to or run against a real submission.
System.Console.WriteLine("warm");
